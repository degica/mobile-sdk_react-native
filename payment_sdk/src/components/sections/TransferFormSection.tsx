import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

type Props = {}

const TransferFormSection = (props: Props) => {
  return (
    <View>
      <Text>TransferFormSection</Text>
    </View>
  )
}

export default TransferFormSection

const styles = StyleSheet.create({})